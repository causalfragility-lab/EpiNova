% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_intervention_functions.R
\name{build_pi_spline}
\alias{build_pi_spline}
\title{Build a natural cubic spline π(t)}
\usage{
build_pi_spline(knot_times, knot_values, extrapolate = c("flat", "linear"))
}
\arguments{
\item{knot_times}{Numeric vector of days (knot positions).}

\item{knot_values}{Numeric vector of pi values at each knot,
all in \eqn{[0, 1]}.}

\item{extrapolate}{How to handle t outside knot range:
\code{"flat"} (default) or \code{"linear"}.}
}
\value{
A function \code{pi_fn(t)}.
}
\description{
Build a natural cubic spline π(t)
}
\examples{
pi_spline <- build_pi_spline(
  knot_times  = c(0, 10, 20, 40, 100),
  knot_values = c(1, 0.8, 0.5, 0.3, 0.3)
)
curve(pi_spline(x), 0, 120, ylab = expression(pi(t)))
}
